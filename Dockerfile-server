# 生产/开发源码镜像，仅包含服务端应用代码
ARG SERVER_BASE_IMAGE=ghcr.io/xinnan-tech/xiaozhi-esp32-server:server-base
FROM ${SERVER_BASE_IMAGE}

ARG SOURCE_REVISION=unknown
LABEL org.opencontainers.image.source="local-workspace" \
      org.opencontainers.image.revision="${SOURCE_REVISION}"

ENV XIAOZHI_SOURCE_REVISION="${SOURCE_REVISION}"

COPY main/xiaozhi-server/ .

# The source/base image may contain this path as a directory. The deployment
# compose mounts a model file here, so remove the stale directory after copy.
RUN rm -rf /opt/xiaozhi-esp32-server/models/SenseVoiceSmall/model.pt

# 安装额外依赖（base image 不包含 redis）
RUN pip install --no-cache-dir "redis>=5.0.0"

# 启动应用
CMD ["python", "app.py"]
